{
  "name": "escadas-root",
  "version": "1.0.0",
  "description": "Root configuration to manage escadas frontend and backend scripts",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
